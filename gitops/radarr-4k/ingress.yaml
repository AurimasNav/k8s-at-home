apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: radarr-4k
  namespace: radarr-4k
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/auth-url: "https://login.sync.lt/oauth2/auth"
    nginx.ingress.kubernetes.io/auth-signin: "https://login.sync.lt/oauth2/start?rd=$scheme://$best_http_host$escaped_request_uri"
spec:
  tls:
  - hosts:
    - radarr-4k.sync.lt
    secretName: radarr-4k-letsencrypt
  rules:
  - host: radarr-4k.sync.lt
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: radarr-4k
            port:
              name: app
