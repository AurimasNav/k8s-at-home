apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: hairsera-microsoft-oauth
  namespace: hairsera
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  refreshInterval: "1800s"
  secretStoreRef:
    name: doppler-secret-store
    kind: ClusterSecretStore
  target:
    name: hairsera-microsoft-oauth
  dataFrom:
  - extract:
      key: MICROSOFT_OAUTH_JSON
