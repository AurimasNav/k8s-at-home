apiVersion: v1
kind: ConfigMap
metadata:
  name: otbr-disable-mdns
  namespace: otbr
data:
  disable-mdns.sh: |
    #!/bin/bash
    echo "INFO: mDNS disabled - port 5353 reserved for Matter Server"
    exec sleep infinity
