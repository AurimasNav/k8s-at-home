apiVersion: v1
kind: Service
metadata:
  labels:
    app: unbound
  name: unbound
  namespace: unbound
spec:
  ports:
  - name: dns-tcp
    port: 53
    protocol: TCP
    targetPort: 53
    appProtocol: domain
  - name: dns-udp
    port: 53
    protocol: UDP
    targetPort: 53
    appProtocol: domain
  selector:
    app: unbound
